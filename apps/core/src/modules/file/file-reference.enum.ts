export enum FileReferenceStatus {
  Pending = 'pending',
  Active = 'active',
  Detached = 'detached',
}

export enum FileReferenceType {
  Post = 'post',
  Note = 'note',
  Page = 'page',
  Draft = 'draft',
  Comment = 'comment',
}
