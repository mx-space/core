import { defineMetadata } from '~/lib/route-meta'

export const metadata = defineMetadata({
  hidden: true,
})

export { BackupDetailRoute as default } from '~/features/backup/components/BackupDetailRoute'
