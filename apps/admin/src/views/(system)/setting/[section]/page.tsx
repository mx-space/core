import { defineMetadata } from '~/lib/route-meta'

export const metadata = defineMetadata({
  hidden: true,
})

export { SettingsDetailRoute as default } from '~/features/settings/components/SettingsDetailRoute'
