import { defineMetadata } from '~/lib/route-meta'

export const metadata = defineMetadata({
  hidden: true,
})

export { EnrichmentDetailRoute as default } from '~/features/enrichment/components/EnrichmentDetailRoute'
