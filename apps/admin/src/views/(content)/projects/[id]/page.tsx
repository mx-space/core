import { defineMetadata } from '~/lib/route-meta'

export const metadata = defineMetadata({
  hidden: true,
})

export { ProjectDetailRoute as default } from '~/features/projects/components/ProjectDetailRoute'
