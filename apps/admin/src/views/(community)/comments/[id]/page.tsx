import { defineMetadata } from '~/lib/route-meta'

export const metadata = defineMetadata({
  hidden: true,
})

export { CommentDetailRoute as default } from '~/features/comments/components/CommentDetailRoute'
