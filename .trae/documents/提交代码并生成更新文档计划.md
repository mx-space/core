我已完成对现有代码变更的分析，以下是执行计划：

## 1. 提交与推送代码
我们将执行以下 Git 操作将您的更新推送到远程仓库：
1.  **添加文件**：将所有修改的文件（包括未追踪的 `apps/core/scripts/download-admin.ejs`）添加到暂存区。
2.  **提交更改**：创建一个包含详细更新说明的提交（Commit）。
    *   提交信息将概括 `UpdateService` 重构和 `PageProxyController` 的优化。
3.  **推送到 GitHub**：将提交推送到 `origin master` 分支。

## 2. 生成更新文档
在推送完成后，我将为您生成一份**精美的 Markdown 更新文档**，包含以下内容：
*   **✨ 核心亮点**：概括本次更新的主要价值。
*   **🚀 功能对比**：使用表格清晰展示“原生 vs 更新后”的差异。
*   **🛠️ 技术细节**：详细说明 `UpdateService` 和 `PageProxyController` 的具体改进（如多镜像源、原子更新、容错机制等）。
*   **📊 变更统计**：代码行数变化概览。

文档将严格遵循您的要求：**全中文、酷炫排版、包含图表与 Emoji**。

请确认是否开始执行提交与推送操作？