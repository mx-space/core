import { ReturnModelType } from '@typegoose/typegoose'

import { SnippetModel } from '../snippet/snippet.model'

export const pourBuiltInFunctions = async (
  model: ReturnModelType<typeof SnippetModel>,
) => {
  // TODO
}
